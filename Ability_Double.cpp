#include "Ability_Double.h"
#include "Ship_Manager.h"
void Ability_Double::using_ability(int* double_damage_switch, int x, int y, Map map, Ship_Manager* ship_manager)
{
	(*double_damage_switch) = 1;
}